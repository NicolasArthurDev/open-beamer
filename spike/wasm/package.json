{
  "name": "@open-beamer/wasm-spike",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Non-blocking Phase 0 exploration: how far an in-browser WASM LaTeX engine compiles the open-beamer feature ladder. Not part of the pnpm workspace; AGPL deps stay isolated here.",
  "scripts": {
    "setup": "npx texlyre-busytex download-assets ./public && npx playwright install chromium",
    "ladder": "node run.mjs"
  },
  "dependencies": {
    "texlyre-busytex": "1.1.1"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
